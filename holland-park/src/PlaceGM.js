import React, { Component } from 'react';
import { InfoWindow, Marker } from 'google-maps-react';

class PlaceGM extends React.Component {


	/*backgroundImage: `url(${})`*/

	 render() {

	  	const { place } = this.props

	    return (
	    	<div>

	     	</div>
	    );
	 }
}

export default PlaceGM;